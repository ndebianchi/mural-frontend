/* Abre Nav (width vira 250px) */
function openNav() {
  document.getElementById("mobileSidebar").style.transform = "translateX(0)";
  }
  
  /* Fecha Nav (width vira 0) */
  function closeNav() {
    document.getElementById("mobileSidebar").style.transform = "translateX(-100%)";
  }

//   segunda linha muda width do "main" pra dar um efeito de deslizar

